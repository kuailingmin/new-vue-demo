import { BASE_URL } from './baseUrl'

// 订单API
const api = {
    login: BASE_URL + '/auth/logout'
}

// 订单API
const apiOne = {
    login: '/auth/logout'
}

const apicom = {
    api,
    apiOne
}

export default apicom
